# Expansion space and foreground resumption — 2026-09-09

Owner clarified that campaign scale requires more territories and neutral land between initial nations. New version-5 maps now use 24 territories per nation (96 for four nations) at the expanded physical dimensions. Starting clusters are planned jointly, preserve four connected regions each, and seek two neutral intervening regions, with a one-neutral fallback. Tiny territory cores receive an initial growth phase; the sampled area-spread test now requires a 3× 80th/20th-percentile difference at the higher density rather than the former 3.5× threshold. This keeps broad variation while avoiding token slivers. Existing maps retain their state.

Owner also reported units racing across the map after returning to a background tab. Added immediate visibility/focus refresh, paused hidden polling/rendering, sequenced overlapping reads and passed an explicit successful-resume signal to the presentation layer. Soldier and vehicle poses snap to authoritative locations on that signal, long frame gaps or stale-snapshot recovery; ordinary foreground updates retain smooth movement. No server persistence or command schema changes were necessary.

Validation: all 13 test files, typecheck and build pass. New tests cover suspended infantry/vehicle snap, explicit resync and normal smoothing afterward, connected initial allocations, neutral gaps and expansion space. The nine spacing fixtures retain two intervening neutral regions. An additional 20-seed spacing audit found no touching starts. Desktop/phone browser checks pass for frozen-tab/focus return, immediate visibility refresh, unchanged camera, new preview counts, campaign geography agreement, zoom and Fit. Tests used isolated in-memory campaigns.
