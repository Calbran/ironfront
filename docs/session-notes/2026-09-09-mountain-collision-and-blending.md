# Mountain collision and terrain blending — 2026-09-09

Implemented decision 020 after the owner requested physical mountain graphics, explicitly exempting air units. Mountain placement/footprints are saved together and rendered from public authoritative map state. Ground movement rejects interiors and swept segment crossings. Pending routes are repaired or stopped. Border routing checks destination-side reachability and tries alternate corridors. Protected bridge passages prevent decoration migration from breaking essential existing land access. Units present during migration keep their positions.

Owner also asked about enriching terrain and pointed out visible repetition. Suggested restrained hills, scrub, tracks, field boundaries and industrial telegraph/power lines; those new decorations were not implemented. Fixed the active tiling issue with a cached 1024px toroidal material compositor: shuffled source crops, quarter-turn rotations, offset positions and feathered radial stamps. Runtime repeat period is four times the previous world length, without mirrored symmetry. No new generated art download.

Validation: typecheck, all 19 test files and production build pass. Mountain tests exercise ground detours/interior rejection, air immunity, stale routes, API rollback and SQLite close/reopen. Existing generated-border routing invariants pass. Browser mountain test selects a real squad, submits an opposite-side move, verifies server path segments avoid the footprint, and confirms an interior click receives HTTP 400. Desktop and phone biome browser checks pass, with reviewed overview/detail material screenshots. Existing build chunk-size advisory remains.
