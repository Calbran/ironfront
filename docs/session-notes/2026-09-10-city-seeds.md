# City seed consistency gallery

Added a sequential twelve-seed capture gallery with six cases, geometry flags and downloadable reports. Added deterministic audit coverage and a 72-case CLI sweep. Exact-seed links retain the stress profile in the diorama, including when varying blocks.

Validation: the 72-case sweep reports zero flags. Browser generated twelve loaded preview pairs, exported JSON and reopened a tight-bend seed with its settings intact; no page errors. Desktop and phone captures are under `.impeccable/review/city-seeds/`. Geometry tests do not replace visual review or validate arbitrary world terrain.
